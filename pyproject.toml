[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "LammpsUtils"
version = "0.0.0"
requires-python = ">= 3.8"
authors = [{name = "Aiden Ochoa", email = "aio5165@psu.edu"}]
readme = "README.md"
license = "MIT"