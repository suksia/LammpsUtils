# ----- initialization ----- #

read_restart cascade.restart

pair_style ?pair_style?
pair_coeff * * ?potential? ?elements?

neighbor ?skin? bin
neigh_modify every 1 delay 0 check yes

# ----- define PKA group ----- #

region pka sphere ?pka_pos? 0.5
group pka region pka

write_dump pka custom pka.dump id type x y z modify append yes

# ----- cascade ----- #

timestep 0.0001

log logs/cascade_?cascade_idx?.log
thermo_style custom step time dt press temp
thermo 1

compute 1 all pe/atom
variable hot atom ?hot_group?

group hot dynamic all var hot every ?snapshot?
dump hot hot custom ?snapshot? cascade.dump id type x y z
dump_modify hot append yes

fix nve all nve
fix bath bath langevin ?temp? ?temp? ?Tdamp? ?lang_seed?

fix adapt_dt all dt/reset 1 ?mindt? ?maxdt? ?maxdr? units box

velocity pka set ?pka_vel?

run ?nsteps?

undump hot
unfix nve
unfix bath
unfix adapt_dt

group pka delete
group hot delete

write_restart cascade.restart

reset_timestep 0
write_dump all custom cascades/cascade_?cascade_idx?.dump id type x y z

# ----- quench post-cascade cells ----- #

log minimize.log
minimize ?etol? ?ftol? ?maxiter? ?maxeval?

variable qstep equal ?cascade_idx?+1
reset_timestep ${qstep}
run 0
write_dump all custom quench.dump id type x y z c_1 modify append yes